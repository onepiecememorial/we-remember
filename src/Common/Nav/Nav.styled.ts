import styled from "styled-components"

export const Header = styled.header` 
    background-color: #272727;
    display: flex;
    >div.link-container {
        padding: 0.938rem;
        font-size: 1.125rem;
        margin-top: 0.688rem;
    }
    >img.gomu-gomu {
        height: 50px;
        width: 50px;
        padding: 10px;
    }
`