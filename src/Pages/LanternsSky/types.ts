export type LostNakama = {
  name: string;
  year: string;
  text: string;
};

export type LanternContent = {
  name: string;
  year: string;
  text: string;
  xPos: number;
};

export type LanternContext = {
  name: string;
  year: string;
  text: string;
};
